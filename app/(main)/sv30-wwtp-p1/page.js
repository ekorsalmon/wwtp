import MeterPageContent from '@/components/MeterPageContent'

export const dynamic = 'force-dynamic'

export default function Sv30WwtpP1Page() {
  return (
    <MeterPageContent
      title="SV30 WWTP (P1)"
      description="Pencatatan SV30 (sludge volume 30 menit) WWTP — WWTP Plant 1."
      meterKeys={['sv30_wwtp']}
    />
  )
}
