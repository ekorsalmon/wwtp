import MeterPageContent from '@/components/MeterPageContent'

export const dynamic = 'force-dynamic'

export default function Sv30WwtpP2Page() {
  return (
    <MeterPageContent
      title="SV30 WWTP (P2)"
      description="Pencatatan SV30 (sludge volume 30 menit) WWTP — WWTP Plant 2."
      meterKeys={['sv30_wwtp_p2']}
    />
  )
}
